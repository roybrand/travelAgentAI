# Pinned to the versions the test suite passes on. An unpinned install once pulled mcp 2.x, which renamed
# FastMCP and stopped the server from starting; upgrade on purpose, run pytest, then bump the pin.
fastapi==0.140.0
uvicorn[standard]==0.51.0
langgraph==1.2.9
mcp[cli]==1.28.1
pydantic==2.13.4
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
cryptography==49.0.0
